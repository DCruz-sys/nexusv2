"""Legacy websocket compatibility shim.

WebSocket routes are now mounted directly by the canonical app in `app.routes.*`.
This module intentionally remains importable to reduce merge/import conflicts.
"""
